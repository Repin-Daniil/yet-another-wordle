worker-threads: 4
worker-fs-threads: 2
logger-level: debug

is-testing: true

# wordle_db is the service name + _ + filename of the db.sql
dbconnection: 'postgresql://testsuite@localhost:15433/wordle_db'
